# Dependencies
node_modules/

# Build output
client/dist/

# Environment secrets (never commit real credentials)
server/.env
.env
.env.local

# Local dev database (SQLite file + WAL/SHM). Generated at runtime (seeded
# by server/src/seed.js) and may contain real student PII; doesn't belong in
# version control. Production uses Turso instead of this local file.
server/data/

# Logs
*.log
.dist/

# Editor/OS cruft
.DS_Store
Thumbs.db

# Claude Code local settings (machine-specific)
.claude/settings.local.json
