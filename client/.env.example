# Only needed for a split deploy where the frontend and backend are on
# different origins (e.g. frontend on Vercel, backend on Render). Leave
# unset for local dev — Vite's dev-server proxy handles relative /api and
# /uploads requests against the local backend instead.
VITE_API_URL=https://your-backend.onrender.com
